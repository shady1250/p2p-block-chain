To use the source code use the following command on command line:
python3 main.py
python main.py

To pass command line arguements just change the variables in the main.py file where the command line arguments name is exactly same as the name of parameters.